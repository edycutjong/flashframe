[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "flashframe"
version = "1.1.0"
description = "Photosensitivity hazard detector"
dependencies = [
    "click",
    "pydantic",
    "google-genai",
    "python-dotenv",
]

[tool.ruff]
line-length = 320

[tool.ruff.lint]
select = ["E", "F"]

[project.scripts]
flashframe = "flashframe.cli:cli"
